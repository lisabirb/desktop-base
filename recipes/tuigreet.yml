modules:
  - type: dnf
    install:
      packages:
        - greetd
        - tuigreet
  - type: systemd
    system:
      enable:
        - greetd
  - type: files
    files:
      - source: tuigreet/greetd.toml
        destination: /etc/greetd/config.toml
