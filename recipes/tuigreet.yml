modules:
  - type: script
    snippets:
      - "dnf -y install greetd tuigreet"
      - "systemctl disable lightdm"
      - "systemctl enable greetd"
  - type: files
    files:
      - source: tuigreet/greetd.toml
        destination: /etc/greetd/config.toml
